{
  "name": "web",
  "version": "1.0.0",
  "description": "",
  "main": "",
  "scripts": {
    "run": "$PROJECT_DIR flutter run web",
    "build": "$PROJECT_DIR flutter build web --release -o ./web/build"
  },
  "author": "pocket-red-fox",
  "license": "MIT",
  "dependencies": {
    "dotenv": "^16.0.3",
    "firebase": "^9.22.1"
  }
}
